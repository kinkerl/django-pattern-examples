# Django Pattern Examples

This repository demonstrates modern Django best practices for building scalable, production-ready applications.
The examples and anti-examples are implemented in test. Look at the `test` folder.

To run the tests:

```
docker compose run pytest
```

## Notes

Some examples rely on a real database and will fail on something like sqlite. 